import React from "react";
import Head from "next/head";

import Footer from "../Footer";
import Navbar from "../Navbar";
import ThemeToggler from "../../src/ThemeToggler";

const Layout = ({
  children,
  className,
}: {
  children: React.ReactNode;
  className: string;
}) => {
  return (
    <div className={`bg-ternary dark:bg-bg dark:text-[#fff] ${className}`}>
      <Head>
        <title>Ridwan Mohamed - Site</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <div>
        <Navbar />
      </div>
      <main>{children}</main>
      <div className="absolute top-20 right-5 md:right-16 lg:right-5 ml-3  bg-[#fff] drop-shadow-md p-3 rounded-md">
      <ThemeToggler />
      </div>
      <div>
        <Footer />
      </div>
    </div>
  );
};

export default Layout;
