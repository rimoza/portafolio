export interface IProjects {
  id: string;
  name: string;
  title: string;
  description: string;
  image: string;
  websiteUrl?: string;
  type?: string;
  imageUrl?: string;
}
export interface ISkills {
  id?: string;
  name: string;
  title?: string;
}
